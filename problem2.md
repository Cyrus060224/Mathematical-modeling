# 问题二：多源异构数据迁移分类与评价系统逻辑解析

> **文档说明**：本阶段利用问题一产出的“黄金标准”（无监督聚类结果），训练监督学习分类器，并引入信息论指标对新流入数据进行异常拦截。
> **核心工具链**：Pandas (多引擎容错), Scikit-learn (Logistic Regression), SciPy (Entropy)

---

## 1. 动态容错数据加载流 (`load_new_datasets`)
**解决逻辑**：现实世界的数据集往往是极其肮脏且格式混乱的（比如假后缀的 CSV、损坏的 Excel）。代码构建了一套“多重引擎降级容错”机制。

* **知识点 1：异常捕获与降级策略 (Try-Except Fallback)**
    在读取数据集 3 时，代码没有盲目相信 `.xlsx` 后缀。
    * **优先级 1**：使用 `openpyxl` 引擎按标准 Excel 解析。
    * **优先级 2**：若报错，说明可能是伪装的 CSV，改用 `utf-8-sig` 解析。
    * **优先级 3**：若再报错，回退到历史遗留系统的 `gbk` 编码解析。
    这种工程设计极大地增强了系统的鲁棒性（Robustness）。
* **知识点 2：底层数据结构拍平 (Flatten)**
    在使用 Pandas 读取时，针对底层 Arrow 引擎的兼容性 Bug，代码使用了 `df.astype(str).to_numpy().flatten()`。将二维表格的行列结构彻底击碎，降维成一维数组，最后拼接成纯文本流。

---

## 2. 监督学习基座训练 (Logistic Regression)
**解决逻辑**：复用第一问建立的特征空间（5000 维 TF-IDF），训练一个能输出概率的线性分类器。

* **知识点：逻辑回归与类权重平衡**
    这里没有选用复杂的深度学习模型，而是用了 `LogisticRegression`。原因在于其**极强的可解释性**和**运算速度**。
    传入的参数 `class_weight='balanced'` 是点睛之笔：因为各类别的文件数量极不均衡（样本倾斜），这个参数会自动给小众类别增加权重，防止模型偏心于大类。

---

## 3. 核心机制：概率分布与异常拦截 (The Reject Option)
**解决逻辑**：面对新流入的、可能完全不属于现有分类体系的文件，模型不能“瞎猜”，必须把它们拦截下来。

```mermaid
graph TD
    A[输入文本特征] --> B(逻辑回归模型)
    B --> C[输出各类别的概率分布 P]
    C --> D{Top1_Prob < 0.40?}
    D -- Yes --> E[拦截: 低置信度]
    D -- No --> F{Margin < 0.15?}
    F -- Yes --> G[拦截: 多类别特征]
    F -- No --> H[自动归类成功]
    C --> I((计算信息熵 Entropy))
    I --> J[宏观系统量化评价]