# 问题三：多场景资源约束优化与决策系统逻辑解析

> **文档说明**：本阶段处于整个流水线的顶层。在已知各类文件的“异常概率”后，基于极其有限的人力与算力资源，制定最优的调度策略，最大化排雷效率。
> **核心知识点**：多目标综合评价模型 (AHP思想)、贪心算法 (Greedy Strategy)、排队论 (Queuing Theory)

---

## 1. 优先级综合评分体系：RUN 模型
**解决逻辑**：机器给出的概率是冷冰冰的，我们需要把它翻译成符合业务直觉的“处理优先级”。代码构建了一个三维度的 RUN 评价模型：

* **R (Risk - 错分风险)**：
    * *逻辑*：置信度越低，模型越“心虚”，错判的风险就越大。
    * *公式*：`risk_score = (1 - 最大置信度) * 100`。
* **U (Urgency - 紧急程度)**：
    * *逻辑*：紧扣业务核心。即便两份文件风险一样，但一份是普通通知，另一份涉及“资金分配”（类别 2、4），显然资金文件的容错率更低，必须优先看。
    * *代码实现*：利用 Pandas 的向量化操作 `df.loc[df['预测类别'].isin([2, 4]), 'urgency_score'] += 50`，对高危类别进行定向加权。
* **N (Necessity - 复核必要性)**：
    * *逻辑*：如果问题二的系统已经明确打上了“无法明确归类”或“多类别特征”的拦截标签，说明机器彻底举白旗了，这类文件必须交给人类擦屁股，赋予最高基础分 70 分。

通过设定权重 $S = 0.4R + 0.3U + 0.3N$，将所有文件算出一个绝对的分数，并**降序排列**。分数越高的，越是随时会引爆的“雷”。

---

## 2. 运筹调度核心：“两头掐尖”策略
**解决逻辑**：面对单日 3800 份的新增数据，在 S1 场景下（人工 200，自动 1200），系统不可能一口气吃下。必须用最聪明的贪心策略来分配资源。

```mermaid
graph LR
    A[全量排序后的文件列车] --> B(最高分段)
    A --> C(灰色中段)
    A --> D(最低分段)
    
    B -- 前 200 份 --> E[🔥 高危拦截: 移交人工复核]
    C -- 中间 2400 份 --> F[⏳ 算力瓶颈: 暂缓处理/积压]
    D -- 倒数 1200 份 --> G[✅ 绝对安全: 移交自动归档]