# 问题一：多源文本聚类与特征提取算法逻辑深度解析

> **文档说明**：本方案旨在解决海量异构文件（PDF, Word, Excel, 图片, TXT）的自动化处理、特征提取及无监督聚类。
> **环境背景**：macOS / VS Code / Python 3.x
> **核心工具链**：EasyOCR, Jieba, Scikit-learn (TF-IDF + K-Means)

---

## 1. 跨平台系统架构与环境自适应
**核心逻辑**：解决开发环境与生产环境路径不一致的“水土不服”问题。

* **路径动态定位**：利用 `pathlib` 库进行 `BASE_DIR` 溯源，配合 `platform.system()` 自动识别 macOS 路径规范。
* **光学字符识别 (OCR) 初始化**：
    * **知识点**：`EasyOCR` 的 GPU/CPU 调度。
    * **实现**：采用全局初始化策略，避免在文件循环中重复加载模型权重（消耗大量显存/内存）。

---

## 2. 多源异构数据提取流水线
**解决逻辑**：将非结构化文件（二进制或标记语言）统一“榨汁”为纯文本字符串。

### 2.1 各格式处理策略
* **PDF 解析**：利用 `pdfplumber` 提取文本。通过 `max_pages=20` 设定边界，防止处理超长文档时内存溢出（OOM）。
* **图像 OCR (关键点)**：
    * **二进制流读取**：使用 `rb` 模式读取图片，规避特定库在处理 macOS 或 Windows 系统中文路径时可能产生的字符编码冲突。
    * **detail=0 模式**：仅保留文本列表，舍弃坐标框数据，降低内存负载。

---

## 3. 自然语言处理 (NLP) 与特征工程
**解决逻辑**：将杂乱的“文字”转化为可用于数学计算的“高维张量”。

### 3.1 数据降噪与清洗
1.  **正则净化 (Regex)**：通过 `[^\u4e00-\u9fa5a-zA-Z]+` 匹配，强制保留汉字与字母，剔除一切标点、数字与乱码符号。
2.  **分词技术 (Jieba)**：
    * **原理**：基于词频词典构建前缀词典，通过有向无环图 (DAG) 结合动态规划寻找最大概率切词方案。
3.  **停用词过滤 (Stop Words)**：物理剔除无实际语义权重但极高频的公文词汇（如：关于、推进、进行）。

### 3.2 TF-IDF 特征空间映射
**数学原理**：$TF-IDF = TF(t, d) \times IDF(t, D)$
* **TF (Term Frequency)**：词频，评估该词在单篇文档的重要性。
* **IDF (Inverse Document Frequency)**：逆文档频率，评估该词在整个资料库的稀缺性。
* **参数配置**：
    * `max_df=0.85`：抑制过度普及的“废话”词汇。
    * `min_df=15`：过滤偶然性极高的极少数干扰项。

---

## 4. K-Means 无监督聚类算法
**解决逻辑**：在未知标签的情况下，利用欧几里得距离实现文件的自动归类。

```mermaid
graph TD
    Start[随机初始化 K 个质心] --> Assign[计算各点到质心的欧氏距离]
    Assign --> Group[文件归属最近质心所属簇]
    Group --> Update[计算簇内平均值更新质心位置]
    Update --> Check{质心位置是否收敛?}
    Check -- No --> Assign
    Check -- Yes --> End[输出聚类标签与核心关键词]